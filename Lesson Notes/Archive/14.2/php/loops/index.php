<?php

// While
$i = 0;
while ($i < 100) {
	echo $i;
	$i++;
}

// For Loop
for ($i = 0; $i < count($array); $i++) {
	echo $array[$i];
}

// For Each (with key)
foreach ($array as $key => $val) {

}

// For Each (without key)
foreach ($array as $val) {

}