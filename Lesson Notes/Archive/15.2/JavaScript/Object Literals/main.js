// A new empty object
var tree = {};

// A new object with a property
var another_tree = {"size": "large"};

// A new object with a property
var another_tree = {"size": "large", "evergreen": true};

// This is the same thing, but prettier
var another_tree = {
    "size": "large",
    "evergreen": true
};

This is the same thing
var another_tree = {
    size: "large",
    evergreen: true
};


