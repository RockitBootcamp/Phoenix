// mysql_close($conn);

  // if ($mysqli->connect_errno) {
  //     echo "Failed to connect to MySQL: (" . $mysqli->connect_errno . ") " . $mysqli->connect_error;
  // } else {
  //  echo "MySQL has connected...";
  // }

  //mysql results from query select * from
  // $resultsSet = $mysqli->query("SELECT * FROM game");


  // define some database entries
  // $title = 'Game 1 Test';
  // $details = 'This is a test of the mysql database.';

  // echo $mysqli->query("show databases");

  // while ($row = $res->fetch_assoc()) {
 //   echo " title = " . $row['title'] . "<br>";
  // }

  // if(!$mysqli->query("insert into user (title, details) values ('$title', '$details')")) {
    // echo "Insertion of user: (" . $mysqli->errno . ") " . $mysqli->error;
  // }

  // echo "<br>Game inserted<br>";

 //  while ($row = $resultsSet->fetch_assoc()) {
 //   echo " title = " . $row['title'] . "<br>";
  // }

  // print_r($res);
